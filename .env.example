# Drizzle
DATABASE_URL=local.db

# Vector store (sqlite-vec) — separate DB to avoid drizzle-kit conflicts
VECTOR_DB_URL=vector.db

# OpenRouter
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Actual Budget (read-only finance tool)
ACTUAL_SERVER_URL=https://your-actual-server.example.com
ACTUAL_PASSWORD=your-actual-password
ACTUAL_SYNC_ID=your-budget-sync-id

# Browser Tools (Playwright)
# No API keys needed — runs a local headless Chromium.
# For browser tools with screenshots, use a vision-capable model:
# e.g. google/gemini-2.0-flash, openai/gpt-4o, anthropic/claude-sonnet-4
